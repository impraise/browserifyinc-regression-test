// This is the entry point of the application. Let's give this a try:
console.log("The application boots!");

var test = require("./dependency");

test("Hello!");

document.write("This was added by the JavaScript!");
